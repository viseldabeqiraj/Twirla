# Auth: GitHub secret AZURE_WEBAPP_PUBLISH_PROFILE = entire *.PublishSettings XML from Azure Portal
# (Web app → Overview → Download publish profile). Do not paste Visual Studio .pubxml contents.
#
# Linux Web Apps: Configuration → Application settings → add WEBSITE_WEBDEPLOY_USE_SCM = true → Save,
# then download the publish profile again. Without this, deploy fails with:
# "Publish profile does not contain kudu URL" / "Failed to fetch credentials from Publish Profile".

name: Deploy Backend to Azure

on:
  push:
    branches:
      - master
    paths:
      - "backend/**"

  workflow_dispatch:

env:
  AZURE_WEBAPP_NAME: twirla
  DOTNET_VERSION: "8.0.x"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore dependencies
        run: dotnet restore
        working-directory: backend

      - name: Publish
        run: dotnet publish Twirla.Api/Twirla.Api.csproj -c Release -o ./publish
        working-directory: backend

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: backend/publish
