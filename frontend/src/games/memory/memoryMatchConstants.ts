/** Fixed memory game size: 8 pairs → 16 cards on a 4×4 grid. */
export const MEMORY_PAIR_COUNT = 8;

/** Default time limit for every shop memory game. */
export const MEMORY_TIME_LIMIT_SECONDS = 60;
